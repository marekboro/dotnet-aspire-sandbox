import { HighlightfunDirective } from './highlightfun.directive';

describe('HighlightfunDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightfunDirective();
    expect(directive).toBeTruthy();
  });
});
