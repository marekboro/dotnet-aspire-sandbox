<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Current Number:" FontSize="14" Margin="0,0,0,5" TextAlignment="Center" />
            <TextBlock Text="{Binding Counter}" FontSize="28" FontWeight="Bold" Margin="0,0,0,25"
                       TextAlignment="Center" />
            <Button
                Content="Add 1"
                Width="180" Height="35"
                Margin="0,0,0,10"
                Click="Add_One_OnClick">

            </Button>
            <Button
                Content="Remove 2"
                Width="180" Height="35"
                Margin="0,0,0,10"
                IsEnabled="{Binding EnabledButtonTwo}"
                Click="Remove_Two_OnClick">
            </Button>
        </StackPanel>

    </Grid>
</Window>